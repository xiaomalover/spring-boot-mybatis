<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weison.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.weison.model.User">
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, user_name, password, phone
    </sql>

    <insert id="insert" parameterType="com.weison.model.User">
        insert into user_info (user_id, user_name, password, phone)
        values (#{userId,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR},#{phone,jdbcType=VARCHAR})
    </insert>


    <select id="queryAll" resultType="com.weison.model.User">
        SELECT  * from user_info
    </select>

</mapper>